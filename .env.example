# Copie para `.env` (não commitado) e ajuste se necessário.
# Consumido em build-time por scripts/generate-environment.mjs, que gera
# src/environments/environment.ts — não existe leitura de .env em runtime,
# porque o build de produção é 100% estático (sem servidor Node).
WEATHER_API_BASE_URL=https://ai-weather-forecasting-omega.vercel.app
