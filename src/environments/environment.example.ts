/**
 * Formato do arquivo gerado por `scripts/generate-environment.mjs` a partir
 * do `.env`. Este arquivo é só documentação — o real (`environment.ts`) é
 * gitignored e recriado a cada `npm start`/`npm run build`.
 */
export const environment = {
  weatherApiBaseUrl: 'https://ai-weather-forecasting-omega.vercel.app',
};
